import React, { Fragment, useState, useEffect } from "react";

const mapStyles = {
    width: '100%',
    height: '100%'
};

const Disasters = ({ floodedLocations }) => {
    return (
        <Fragment>
            <div className="container">
                <h4>Disasters Page</h4>
            </div>
        </Fragment>
    );
}

export default Disasters;